% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inflated.R
\name{fit_inflated_mpl}
\alias{fit_inflated_mpl}
\title{Fit inflated interval model by modified profile likelihood}
\usage{
fit_inflated_mpl(
  Y,
  ITEM_INDS,
  J,
  REF_FIT = NULL,
  START = NULL,
  METHOD = "BFGS",
  CONTROL = list(),
  COMPUTE_VCOV = TRUE,
  HESSIAN_STEP = 1e-04,
  LOWER_ALPHA = -5,
  UPPER_ALPHA = 5,
  PROF_MAX_ITER = 500L,
  BOUNDARY = 100
)
}
\arguments{
\item{Y}{Numeric vector of responses in [0,1].}

\item{ITEM_INDS}{Integer vector of item indices (1-indexed, length equal to Y).}

\item{J}{Number of items.}

\item{REF_FIT}{Optional profile likelihood fit from \code{\link[=fit_inflated_profile]{fit_inflated_profile()}}.
If \code{NULL}, it is computed automatically.}

\item{START}{Named starting values on the unconstrained scale. If \code{NULL},
initialized from \code{REF_FIT}.}

\item{METHOD}{Optimization method passed to \code{\link[=optim]{optim()}}. Default \code{"BFGS"}.}

\item{CONTROL}{List of control parameters passed to \code{\link[=optim]{optim()}}.}

\item{COMPUTE_VCOV}{Logical. If \code{TRUE}, compute vcov and standard errors.}

\item{HESSIAN_STEP}{Step size for numerical Hessian when numDeriv is unavailable.}

\item{LOWER_ALPHA}{Lower bound for per-item alpha search. Default -5.}

\item{UPPER_ALPHA}{Upper bound for per-item alpha search. Default 5.}

\item{PROF_MAX_ITER}{Maximum Brent iterations per item. Default 500.}

\item{BOUNDARY}{Boundary value used to pin the fixed cutpoint. Default 100.}
}
\value{
A list of class \code{"inflated_fit"} with components \code{phi}, \code{k0}, \code{k1},
\code{alpha}, \code{loglik}, \code{profile_loglik}, \code{correction}, \code{vcov}, \code{se},
\code{convergence}, and others.
}
\description{
Estimates (phi, k0, k1) using the Barndorff-Nielsen modified profile
likelihood. A profile likelihood fit is first obtained (or supplied via
\code{REF_FIT}) and used as the MLE reference for the BN correction.
}
