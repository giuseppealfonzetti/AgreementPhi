% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/control.R
\name{rating_data}
\alias{rating_data}
\title{Prepare rating data for analysis}
\usage{
rating_data(
  RATINGS,
  ITEM_INDS,
  WORKER_INDS = NULL,
  ITEM_LABELS = NULL,
  WORKER_LABELS = NULL,
  K = NULL,
  VERBOSE = FALSE
)
}
\arguments{
\item{RATINGS}{Ratings vector. Ordinal: integers in \{1,...,K\}. Continuous:
reals in \verb{(0,1)}. Inflated interval: reals in \verb{[0,1]} with exact 0s or 1s.}

\item{ITEM_INDS}{Integer index vector of item allocations (same length as \code{RATINGS}).}

\item{WORKER_INDS}{Integer index vector of worker allocations.}

\item{ITEM_LABELS}{Optional character vector of item labels (same length as \code{RATINGS}).
Each unique item index must map to exactly one label. When provided, label names
are used for \code{alpha} coefficients in \code{\link[=coef]{coef()}}.}

\item{WORKER_LABELS}{Optional character vector of worker labels (same length as \code{RATINGS}).
Requires \code{WORKER_INDS}. When provided, label names are used for \code{beta} coefficients in \code{\link[=coef]{coef()}}.}

\item{K}{Number of ordinal categories. If \code{NULL}, inferred as \code{max(RATINGS)}.
Provide explicitly when boundary categories may be absent from the data.}

\item{VERBOSE}{Print data diagnostics on construction. Default \code{TRUE}.}
}
\value{
An S3 object of class \code{rating_data}.
}
\description{
Validates and preprocesses a raw ratings dataset. Returns a \code{rating_data}
S3 object that can be passed to \code{\link[=agreement]{agreement()}}, \code{\link[=plot]{plot()}}, and \code{\link[=print]{print()}}.
Degenerate items (all ratings identical) are detected and their recoded
indices stored in \verb{$degen_ids}; no observations are removed here.
The decision to drop degenerate items before fitting is delegated to \code{\link[=agreement]{agreement()}}.
}
\examples{
dt <- sim_data(J = 20, B = 5, AGREEMENT = 0.6,
               ALPHA = rep(0, 20), DATA_TYPE = "continuous", SEED = 1)
rd <- rating_data(dt$rating, dt$id_item, dt$id_worker)
print(rd)

}
