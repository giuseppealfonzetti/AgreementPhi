% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{coef.agreement_fit}
\alias{coef.agreement_fit}
\title{Extract coefficients from an agreement fit}
\usage{
\method{coef}{agreement_fit}(object, ...)
}
\arguments{
\item{object}{An \code{agreement_fit} object from \code{\link[=agreement]{agreement()}}.}

\item{...}{Ignored.}
}
\value{
A named numeric vector. Always contains \code{phi} and \code{alpha_1...alpha_J}.
For inflated data: also \code{k0} and \code{k1}. For two-way models (workers profiled
as nuisance): also \code{beta_1...beta_W}.
}
\description{
Extract coefficients from an agreement fit
}
\examples{
set.seed(1)
dt <- sim_data(J = 20, B = 5, AGREEMENT = 0.6,
               ALPHA = rep(0, 20), DATA_TYPE = "continuous", SEED = 1)
rd <- rating_data(dt$rating, dt$id_item, dt$id_worker, VERBOSE = FALSE)
fit <- agreement(rd, METHOD = "modified", NUISANCE = "items")
coef(fit)

}
